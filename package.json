{
  "name": "healthcore-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "HealthCore — TypeScript utilities, landing pages, and patient enquiry form",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "serve": "npx --yes http-server . -p 3000 -a 0.0.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}